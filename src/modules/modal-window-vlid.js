export function ValidModal(){
    // let phones_users=document.querySelectorAll('.phone-user');
    // // let name_users=document.querySelectorAll('')
    // let num_pattern=/^[1-9]+[0-9]*$/;
    // phones_users.forEach((item)=>{
    //     item.addEventListener('input',()=>{
    //         if(!num_pattern.test(item.value[item.value.lenth-1])){
    //             item.value=item.value.slice(0,-1)
    //         }
    //     })
    // })

}